#include<stdio.h>
#include<string.h>
int main()
{
    char a[100];
    int i,j=0;
    scanf("%s",a);
    for(i=0;i<strlen(a);i++)
    {
        if(!(a[i]>='0' && a[i]<='9'))
            {
                j=1;
                break;
            }
    }
    if(j==0)
        printf("Integer");
    else
        printf("String");
}
